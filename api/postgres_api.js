
// return an array of landlord_ids
modules.grabAllLandlords = function(landlord_id, avgTime) {
  const p = new Promise((res, rej) => {

  })
  return p
}

// update the avgTime of response for a landlord
modules.updateTypicalResponseTime = function(landlord_id, avgTime) {
  const p = new Promise((res, rej) => {

  })
  return p
}
